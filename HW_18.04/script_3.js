function min(a, b) {
    if (a < b) {
      return a;
    } else {
      return b;
    }
  }
  min(17, 29) //числа для проверки подставила//


function max(a, b) {
    if (a > b) {
      return a;
    } else {
      return b;
    }
  }
  max(58, 33) //числа для проверки подставила//




  //Часть вторая задания//

let a, b;

function min(a, b) {
    return a < b ? a : b;
  }
  min(17, 29) //числа для проверки подставила//

function max(a, b) {
    return a > b ? a : b;
  }
  max(58, 33) //числа для проверки подставила//

